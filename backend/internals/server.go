package server

// Deprecated: this package's Run() was the old entry point; the real one
// now lives directly in cmd/server/main.go (matching the laffaire/
// go-web-template pattern this backend was rebuilt on). Kept only because
// this session couldn't get permission to delete the file outright - safe
// to remove.

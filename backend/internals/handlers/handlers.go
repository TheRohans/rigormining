package handlers

// Deprecated: the old html/template page-serving handlers (ServePage,
// TemplateInit) were removed - the React SPA now serves the UI, via
// handlers.SPAFileServer (see static.go). Kept only because this session
// couldn't get permission to delete the file outright - safe to remove.
